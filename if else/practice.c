#include<stdio.h>
void main()
{
     char a,b;
     printf("enter:");
     scanf("%s%s",&a,&b);
     if(a==b)
     {
         printf("both are same");

     }
    else
    {
        printf("both are different");
    }
    return 0;
}
